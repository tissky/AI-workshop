# Vercel 部署指南

本指南详细介绍如何使用自动化设置脚本和手动方式将 AI创意工坊 部署到 Vercel。

## 目录

- [快速开始](#快速开始)
- [前置要求](#前置要求)
- [使用自动化设置脚本](#使用自动化设置脚本)
- [手动部署流程](#手动部署流程)
- [环境变量配置](#环境变量配置)
- [部署选项](#部署选项)
- [常见问题](#常见问题)
- [回滚与故障排除](#回滚与故障排除)

---

## 快速开始

### 方法一：自动化设置脚本（推荐）

```bash
# 克隆项目
git clone https://github.com/yourusername/ai-workshop.git
cd ai-workshop

# 运行自动化设置脚本
npm run setup

# 按照向导提示完成配置
```

### 方法二：一键部署按钮

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/yourusername/ai-workshop)

点击按钮后，Vercel 会自动：
1. Fork 仓库到你的 GitHub 账户
2. 创建新的 Vercel 项目
3. 配置自动部署
4. 部署到生产环境

---

## 前置要求

### 必需环境

| 工具 | 最低版本 | 推荐版本 | 安装方法 |
|------|---------|---------|----------|
| **Node.js** | 18.0.0 | 20.x LTS | [nodejs.org](https://nodejs.org) |
| **npm** | 9.0.0 | 最新版本 | 随 Node.js 安装 |
| **Git** | 2.0+ | 最新版本 | [git-scm.com](https://git-scm.com) |

### 可选工具

| 工具 | 用途 | 安装方法 |
|------|------|----------|
| **Vercel CLI** | 命令行部署 | `npm install -g vercel` |

### 账户要求

- **Vercel 账户**：[vercel.com](https://vercel.com) 免费注册
- **GitHub 账户**（推荐）：用于自动部署

---

## 使用自动化设置脚本

### 脚本功能

`scripts/setup.sh` 自动化脚本提供以下功能：

1. ✅ **环境验证**
   - 检查 Node.js 版本（>= 18.0.0）
   - 检查 npm 版本
   - 验证环境兼容性

2. 📦 **依赖管理**
   - 自动安装 npm 依赖
   - 显示安装进度

3. 🔧 **Vercel CLI 检测**
   - 检测是否已安装 Vercel CLI
   - 提供交互式安装选项
   - 可跳过，稍后手动安装

4. ⚙️ **环境配置**
   - 从 `.env.example` 创建 `.env.local`
   - 交互式配置网站 URL
   - 配置可选功能（Bundle 分析器等）
   - 自动备份现有配置

5. 🏗️ **构建验证**
   - 可选的生产构建测试
   - 验证配置正确性

6. 🚀 **部署选项**
   - 部署到预览环境
   - 部署到生产环境
   - 跳过部署，稍后手动操作

### 使用步骤

#### 1. 运行设置脚本

```bash
npm run setup
```

#### 2. 按照向导提示操作

脚本会引导你完成以下步骤：

**步骤 1：环境验证**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  第一步: 验证环境
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ℹ 检查Node.js版本...
ℹ 当前Node.js版本: v20.19.5
✓ Node.js版本符合要求 (>= 18.0.0)
✓ npm已安装
```

**步骤 2：安装依赖**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  第二步: 安装依赖
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ℹ 正在安装npm依赖包...
⚠ 这可能需要几分钟时间，请耐心等待...
✓ 依赖安装成功！
```

**步骤 3：Vercel CLI**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  第三步: 检查Vercel CLI
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠ 未检测到Vercel CLI
是否安装Vercel CLI？这将允许你使用命令行部署项目 [y/N]: y
```

**步骤 4：配置环境变量**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  第四步: 配置环境变量
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✓ 已从.env.example创建.env.local文件
请输入网站URL（留空使用localhost:3000）: https://ai-workshop.vercel.app
✓ 网站URL已设置为: https://ai-workshop.vercel.app
```

**步骤 5：构建验证**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  第五步: 验证构建
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

是否运行构建测试以验证配置？[Y/n]: y
✓ 构建成功！项目配置正确
```

**步骤 6：部署**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  第六步: 部署选项
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  1) 立即部署到Vercel预览环境
  2) 立即部署到Vercel生产环境
  3) 跳过，稍后手动部署

请选择 [1-3]: 1
```

#### 3. 查看配置摘要

脚本完成后会显示配置摘要：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  配置摘要
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Node.js版本:     v20.19.5
  npm版本:         11.6.2
  网站URL:         https://ai-workshop.vercel.app
  Vercel CLI:      ✓ 已安装
  环境配置:        .env.local

🎉 祝你开发愉快！
```

### 脚本选项

脚本提供多个交互式选项：

| 选项 | 默认值 | 说明 |
|------|-------|------|
| 安装 Vercel CLI | N (跳过) | 是否全局安装 Vercel CLI |
| 覆盖现有 .env.local | N (保留) | 如果文件已存在，是否覆盖 |
| 网站 URL | localhost:3000 | 配置 NEXT_PUBLIC_SITE_URL |
| 启用 Bundle 分析 | N (禁用) | 是否启用 webpack bundle analyzer |
| 运行构建测试 | Y (执行) | 是否验证生产构建 |
| 部署选项 | 3 (跳过) | 选择部署方式 |

---

## 手动部署流程

如果不使用自动化脚本，可以按照以下步骤手动部署。

### 方法一：Vercel CLI 部署

#### 1. 安装 Vercel CLI

```bash
npm install -g vercel
```

#### 2. 登录 Vercel

```bash
vercel login
```

选择登录方式：
- GitHub
- GitLab
- Bitbucket
- Email

#### 3. 初始化项目

在项目根目录运行：

```bash
vercel
```

按照提示操作：
```
? Set up and deploy "~/ai-workshop"? [Y/n] y
? Which scope do you want to deploy to? Your Name
? Link to existing project? [y/N] n
? What's your project's name? ai-workshop
? In which directory is your code located? ./
Auto-detected Project Settings (Next.js):
- Build Command: next build
- Development Command: next dev --port $PORT
- Install Command: npm install
- Output Directory: Next.js default
? Want to modify these settings? [y/N] n
```

#### 4. 配置环境变量

```bash
# 设置生产环境变量
vercel env add NEXT_PUBLIC_SITE_URL production
# 输入你的生产URL，如: https://ai-workshop.vercel.app

# 设置预览环境变量
vercel env add NEXT_PUBLIC_SITE_URL preview
# 输入预览URL或生产URL
```

#### 5. 部署到预览环境

```bash
vercel
```

部署成功后会显示预览 URL：
```
🔍 Inspect: https://vercel.com/yourname/ai-workshop/xxx
✅ Preview: https://ai-workshop-xxx.vercel.app
```

#### 6. 部署到生产环境

验证预览环境无误后：

```bash
vercel --prod
```

### 方法二：GitHub + Vercel 集成（推荐）

#### 1. 推送代码到 GitHub

```bash
# 初始化Git仓库（如果尚未初始化）
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit"

# 创建main分支
git branch -M main

# 添加远程仓库
git remote add origin https://github.com/yourusername/ai-workshop.git

# 推送到GitHub
git push -u origin main
```

#### 2. 连接 Vercel

1. 访问 [vercel.com](https://vercel.com)
2. 点击 **"Add New Project"**
3. 选择 **"Import Git Repository"**
4. 授权并选择你的 GitHub 仓库
5. 配置项目：

**项目设置：**
```
Framework Preset: Next.js
Root Directory: ./
Build Command: next build
Output Directory: .next
Install Command: npm install
```

**环境变量：**
```
NEXT_PUBLIC_SITE_URL = https://ai-workshop.vercel.app
ANALYZE = false
```

6. 点击 **"Deploy"**

#### 3. 自动部署配置

Vercel 会自动为以下事件部署：

| 事件 | 环境 | URL |
|------|------|-----|
| Push to `main` | Production | your-domain.vercel.app |
| Pull Request | Preview | pr-123-xxx.vercel.app |
| Push to other branches | Preview | branch-xxx.vercel.app |

### 方法三：Vercel Dashboard 手动上传

1. 访问 [vercel.com/new](https://vercel.com/new)
2. 选择 **"Browse"** 上传项目文件夹
3. 配置项目设置（同方法二）
4. 点击 **"Deploy"**

**注意：** 此方法不推荐用于生产环境，因为无法自动化更新。

---

## 环境变量配置

### 必需环境变量

| 变量名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| `NEXT_PUBLIC_SITE_URL` | 公开 | 网站完整URL，用于SEO和sitemap | `https://ai-workshop.vercel.app` |

### 可选环境变量

| 变量名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `ANALYZE` | 私有 | `false` | 启用webpack bundle分析 |

### 配置方式

#### 本地开发

编辑 `.env.local` 文件：

```bash
NEXT_PUBLIC_SITE_URL=http://localhost:3000
ANALYZE=false
```

#### Vercel Dashboard

1. 打开项目设置：`Settings` → `Environment Variables`
2. 添加变量：
   - **Key:** `NEXT_PUBLIC_SITE_URL`
   - **Value:** `https://your-domain.vercel.app`
   - **Environments:** 选择 `Production`, `Preview`, `Development`
3. 点击 **"Save"**

#### Vercel CLI

```bash
# 添加生产环境变量
vercel env add NEXT_PUBLIC_SITE_URL production

# 添加预览环境变量
vercel env add NEXT_PUBLIC_SITE_URL preview

# 查看所有环境变量
vercel env ls

# 删除环境变量
vercel env rm NEXT_PUBLIC_SITE_URL production
```

### 环境变量优先级

1. Vercel Dashboard/CLI 设置（最高优先级）
2. `.env.local`
3. `.env.production` / `.env.development`
4. `.env`

---

## 部署选项

### 预览部署（Preview Deployment）

**特点：**
- 每个 Pull Request 自动创建独立预览环境
- 非 main 分支推送会创建预览部署
- 适合测试新功能和代码审查

**命令：**
```bash
vercel
```

**URL 格式：**
```
https://ai-workshop-git-feature-branch-yourname.vercel.app
https://ai-workshop-pr-123-yourname.vercel.app
```

### 生产部署（Production Deployment）

**特点：**
- 绑定到自定义域名
- Push to `main` 分支自动部署
- 最稳定的部署环境

**命令：**
```bash
vercel --prod
```

**URL：**
```
https://your-domain.vercel.app
https://your-custom-domain.com
```

### 预构建部署（Prebuilt Deployment）

**适用场景：**
- 在 CI/CD 流程中预先构建
- 需要自定义构建步骤
- 构建时间优化

**步骤：**
```bash
# 1. 本地构建
npm run build

# 2. 部署预构建产物
vercel --prebuilt
```

---

## 常见问题

### Q1: 构建失败，提示"Module not found"

**原因：** 缺少依赖或依赖未正确安装

**解决方案：**
```bash
# 清理缓存
rm -rf node_modules package-lock.json

# 重新安装
npm install

# 重新构建
npm run build
```

### Q2: 环境变量未生效

**原因：** 环境变量配置错误或未重新部署

**解决方案：**
1. 确认变量名正确（`NEXT_PUBLIC_` 前缀必需）
2. 在 Vercel Dashboard 检查变量配置
3. 触发重新部署：
   ```bash
   vercel --prod --force
   ```

### Q3: 404 错误：动态路由无法访问

**原因：** 静态导出配置错误

**解决方案：**
确保 `next.config.ts` 中未启用 `output: 'export'`。Vercel 支持服务端渲染。

### Q4: 图片加载失败

**原因：** Next.js Image 配置问题

**解决方案：**
检查 `next.config.ts` 中的 `images` 配置：
```typescript
images: {
  formats: ["image/avif", "image/webp"],
  deviceSizes: [640, 750, 828, 1080, 1200, 1920, 2048, 3840],
}
```

### Q5: 部署时间过长

**原因：** 依赖安装或构建优化不足

**解决方案：**
1. 启用 Vercel 缓存
2. 优化依赖：
   ```bash
   # 使用 npm ci 代替 npm install
   npm ci
   ```
3. 分析 Bundle 大小：
   ```bash
   npm run build:analyze
   ```

### Q6: Vercel CLI 登录失败

**原因：** 网络问题或认证过期

**解决方案：**
```bash
# 清除认证信息
vercel logout

# 重新登录
vercel login
```

---

## 回滚与故障排除

### 版本回滚

#### 通过 Vercel Dashboard

1. 打开项目页面
2. 点击 **"Deployments"** 标签
3. 找到稳定的历史部署
4. 点击 **"⋯"** → **"Promote to Production"**

#### 通过 Vercel CLI

```bash
# 查看部署历史
vercel ls

# 回滚到特定部署
vercel rollback <deployment-url>
```

### 回滚到上一个稳定版本

```bash
# Git回滚
git revert HEAD
git push origin main

# 或者使用Git reset（谨慎使用）
git reset --hard HEAD~1
git push origin main --force
```

### 紧急回滚流程

1. **识别问题：** 确认生产环境故障
2. **暂停部署：** Vercel Dashboard → Settings → Git → 暂停自动部署
3. **回滚版本：** 使用上述回滚方法
4. **验证修复：** 访问生产URL确认问题解决
5. **恢复部署：** 重新启用自动部署

### 诊断工具

#### 1. Vercel 日志查看

```bash
# 查看最新部署日志
vercel logs

# 查看特定部署日志
vercel logs <deployment-url>

# 实时日志流
vercel logs --follow
```

#### 2. 本地调试生产构建

```bash
# 构建生产版本
npm run build

# 本地运行生产服务器
npm start

# 访问 http://localhost:3000 验证
```

#### 3. 检查环境变量

```bash
# 列出所有环境变量
vercel env ls

# 拉取环境变量到本地
vercel env pull .env.local
```

### 常见错误代码

| 错误代码 | 说明 | 解决方案 |
|----------|------|----------|
| **500** | 服务器内部错误 | 检查服务端代码和日志 |
| **502** | 网关错误 | 检查API路由和外部服务 |
| **504** | 请求超时 | 优化慢查询和API调用 |
| **Build Error** | 构建失败 | 查看构建日志，修复代码错误 |

### 性能监控

#### Vercel Analytics

在 Vercel Dashboard 启用 Analytics：
1. 项目页面 → **"Analytics"** 标签
2. 点击 **"Enable Analytics"**
3. 查看实时性能指标

#### 自定义监控

```javascript
// pages/_app.tsx
export function reportWebVitals(metric) {
  console.log(metric);
  // 发送到你的分析服务
}
```

---

## 最佳实践

### 1. 使用自动化脚本

始终使用 `npm run setup` 进行初始配置，确保环境一致性。

### 2. 环境分离

- **开发环境：** 使用 `.env.local`
- **预览环境：** Vercel 自动部署 PR
- **生产环境：** 仅从 `main` 分支部署

### 3. 版本控制

- 不要提交 `.env.local` 到 Git
- 始终保持 `.env.example` 更新
- 使用语义化版本标签

### 4. 监控与告警

- 启用 Vercel Analytics
- 配置部署通知（Slack, Email）
- 定期检查错误日志

### 5. 性能优化

- 使用 ISR (Incremental Static Regeneration)
- 启用图片优化
- 分析和优化 Bundle 大小

---

## 延伸阅读

- [Vercel 官方文档](https://vercel.com/docs)
- [Next.js 部署文档](https://nextjs.org/docs/deployment)
- [Vercel CLI 文档](https://vercel.com/docs/cli)
- [环境变量最佳实践](https://vercel.com/docs/concepts/projects/environment-variables)

---

## 技术支持

如遇到问题，请：

1. 查阅本文档的 [常见问题](#常见问题) 部分
2. 查看 [Vercel 文档](https://vercel.com/docs)
3. 访问项目 GitHub Issues
4. 联系技术支持团队

---

**文档版本：** v1.0.0  
**最后更新：** 2024-11  
**维护者：** AI创意工坊技术团队
