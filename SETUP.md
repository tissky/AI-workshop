# 🚀 项目设置指南

快速配置 AI创意工坊 项目的自动化指南。

## 快速开始

### 一键自动化设置

```bash
npm run setup
```

这个命令会启动交互式设置向导，自动完成以下任务：

✅ **环境验证** - 检查 Node.js (>= 18.0) 和 npm 版本  
✅ **依赖安装** - 自动安装所有 npm 依赖包  
✅ **Vercel CLI** - 检测/安装 Vercel 命令行工具（可选）  
✅ **环境配置** - 创建 `.env.local` 并配置环境变量  
✅ **构建测试** - 验证生产构建是否成功（可选）  
✅ **部署选项** - 直接部署到 Vercel 预览或生产环境（可选）

---

## 详细文档

### 📚 完整部署指南
**[docs/deployment/vercel.md](./docs/deployment/vercel.md)**

包含以下内容：
- 自动化设置脚本完整说明
- 手动部署方法（Vercel CLI、GitHub 集成、Dashboard）
- 环境变量详细配置
- 部署策略（预览/生产/预构建）
- 常见问题解答
- 回滚和故障排除

### 🎯 脚本功能详解

#### 第一步：环境验证
- 检查 Node.js 版本（最低要求 18.0.0）
- 检查 npm 可用性
- 显示当前版本信息

#### 第二步：安装依赖
- 自动运行 `npm install`
- 显示安装进度
- 错误处理和提示

#### 第三步：Vercel CLI 检测
- 检测系统中是否已安装 Vercel CLI
- 如果未安装，询问是否安装
- 提供跳过选项

#### 第四步：环境配置
- 检查是否存在 `.env.local` 文件
- 如存在，提供备份和覆盖选项
- 从 `.env.example` 创建配置文件
- 交互式配置 `NEXT_PUBLIC_SITE_URL`
- 可选配置 Bundle 分析器

#### 第五步：构建验证
- 可选的生产构建测试
- 运行 `npm run build`
- 验证配置是否正确

#### 第六步：部署选项
- 选项 1：部署到 Vercel 预览环境
- 选项 2：部署到 Vercel 生产环境
- 选项 3：跳过部署，稍后手动操作

---

## 环境变量

### 必填变量

| 变量名 | 说明 | 示例 |
|--------|------|------|
| `NEXT_PUBLIC_SITE_URL` | 网站完整URL，用于SEO和sitemap | `https://ai-workshop.vercel.app` |

### 可选变量

| 变量名 | 默认值 | 说明 |
|--------|--------|------|
| `ANALYZE` | `false` | 启用webpack bundle分析器 |

### 配置方法

**开发环境：**
```bash
NEXT_PUBLIC_SITE_URL=http://localhost:3000
ANALYZE=false
```

**生产环境：**
```bash
NEXT_PUBLIC_SITE_URL=https://your-domain.vercel.app
```

---

## 常用命令

### 开发命令
```bash
npm run dev           # 启动开发服务器
npm run build         # 构建生产版本
npm run start         # 运行生产服务器
npm run lint          # 代码检查
```

### 分析命令
```bash
npm run build:analyze # 分析Bundle大小（需启用ANALYZE）
```

### 部署命令
```bash
vercel                # 部署到预览环境
vercel --prod         # 部署到生产环境
vercel --prebuilt     # 部署预构建产物
```

---

## 手动设置（不使用脚本）

如果不使用自动化脚本，按照以下步骤手动设置：

### 1. 安装依赖
```bash
npm install
```

### 2. 配置环境变量
```bash
cp .env.example .env.local
# 编辑 .env.local 文件，填写实际值
```

### 3. 验证构建
```bash
npm run build
```

### 4. 启动开发服务器
```bash
npm run dev
```

---

## 部署选项

### 选项 1：自动化脚本部署（推荐）
```bash
npm run setup
# 在第六步选择部署选项
```

### 选项 2：Vercel CLI 部署
```bash
npm install -g vercel
vercel login
vercel
```

### 选项 3：GitHub + Vercel 集成
1. 推送代码到 GitHub
2. 在 Vercel 中连接 GitHub 仓库
3. 自动部署

### 选项 4：一键部署按钮
[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/yourusername/ai-workshop)

---

## 故障排除

### Q: 脚本执行失败
**A:** 检查以下几点：
- 确保 Node.js >= 18.0.0
- 确保有网络连接
- 删除 `node_modules` 和 `package-lock.json` 后重试

### Q: 构建失败
**A:** 运行以下命令清理并重建：
```bash
rm -rf .next node_modules package-lock.json
npm install
npm run build
```

### Q: 环境变量未生效
**A:** 确保：
- 变量名拼写正确
- 变量前缀为 `NEXT_PUBLIC_`（如果需要在客户端访问）
- 重启开发服务器

### Q: Vercel CLI 安装失败
**A:** 尝试使用 sudo（Linux/macOS）：
```bash
sudo npm install -g vercel
```

或者使用 Vercel 的独立安装程序：
```bash
npm i -g vercel@latest
```

---

## 获取帮助

- 📚 查看完整文档：[docs/deployment/vercel.md](./docs/deployment/vercel.md)
- 📖 项目主文档：[README.md](./README.md)
- 🐛 报告问题：在 GitHub Issues 中提交
- 💬 技术支持：联系项目团队

---

**快速链接：**
- [📚 完整部署指南](./docs/deployment/vercel.md)
- [📖 项目文档索引](./docs/README.md)
- [🎨 设计系统](./docs/design-system/)
- [🧩 组件文档](./docs/components/)

---

**最后更新：** 2024-11  
**版本：** 1.0.0
