# Environment Variables Example
# Copy this file to .env.local and update with your values

# ============================================================================
# SITE CONFIGURATION
# ============================================================================

# Canonical site URL (required for SEO, metadata, sitemap)
# Used in: lib/metadata.ts, app/sitemap.ts, app/robots.ts
NEXT_PUBLIC_SITE_URL=https://ai-workshop.example.com

# ============================================================================
# BUILD CONFIGURATION
# ============================================================================

# Enable webpack bundle analyzer (optional)
# Usage: ANALYZE=true npm run build
# Opens interactive treemap at http://localhost:8888
ANALYZE=false

# ============================================================================
# NOTES
# ============================================================================

# Public variables (NEXT_PUBLIC_*):
# - Exposed to browser and server
# - Use for non-sensitive configuration only
# - Must restart dev server after changes

# Private variables (no prefix):
# - Server-side only
# - Safe for API keys and secrets
# - Not accessible in browser

# For more information, see:
# docs/configuration/environment.md
